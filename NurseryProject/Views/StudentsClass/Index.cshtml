@model IEnumerable<NurseryProject.Dtos.StudentsClass.StudentsClassDto>
@using NurseryProject.Utilities
@{
    ViewBag.Title = "ادارة التحاق الطلاب  ";
}
<div class="card">
    <div class="card-header">

        <div class="row">
            <h2 class="col">  </h2>
            <p class="col-auto">
                @Url.Create()
            </p>
        </div>
    </div>
    <div class="card-body">
        <div class="row mt-3">
            @*<div class="form-group mb-3 col-sm-6">
                    @Html.Label("مكان الدراسة", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("StudyPlaceId", new SelectList(""), "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("StudyPlaceId", "", new { @class = "text-danger" })
                    </div>
                </div>*@
            @*<div class="form-group mb-3 col-sm-6">
                    @Html.Label("العام الدراسي", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("StudyYearId", null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("StudyYearId", "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("نوع الدراسة", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("StudyTypeId", null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("StudyTypeId", "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("الصف الدراسي", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("LevelId", null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("LevelId", "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("الفصل", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("ClassId", null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("ClassId", "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("طريقة الاشتراك", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownList("SubscriptionId", null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessage("SubscriptionId", "", new { @class = "text-danger" })
                    </div>
                </div>*@

        </div>
        @*<div class="row justify-content-end">
                <a href="/StudentsClass/Search" class='btn btn-primary  waves-effect waves-light px-3 m-1 col-12 col-md-auto'><i class='fas fa-search me-1'></i>عرض</a>
            </div>
            <hr />*@
        <table id="datatable" class="table table-striped dt-responsive  nowrap w-100">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("الفرع ")
                    </th>
                    <th>
                        @Html.DisplayName("العام الدراسي")
                    </th>
                    <th>
                        @Html.DisplayName("نوع الدراسة")
                    </th>
                    <th>
                        @Html.DisplayName("الصف الدراسي")
                    </th>

                    <th>
                        @Html.DisplayName("الفصل ")
                    </th>

                    <th>
                        @Html.DisplayName("الطالب ")
                    </th>
                    <th>
                        @Html.DisplayName("طريقة الاشتراك ")
                    </th>
                    <th>
                        @Html.DisplayName("مبلغ الاشتراك ")
                    </th>
                    <th>
                        @Html.DisplayName("عدد الاقساط ")
                    </th>
                    <th>
                        @Html.DisplayName("تاريخ الالتحاق ")
                    </th>
                    <th>
                        @Html.DisplayName("ملاحظات ")
                    </th>
                    <th>
                        @Html.DisplayName("عمليات")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudyPlaceName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudyYearName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudyTypeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LevelName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ClassName)

                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudentName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SubscriptionName)
                    </td>
                    <td>
                        @item.Amount جنيه
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Number)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.JoiningDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Notes)
                    </td>
                    <td>
                        @if (item.SubscriptionMethod.Count() > 0)
                        {
                            <a class='btn btn-soft-warning waves-effect waves-light p-2' href="/Subscriptions/Collect/@item.Id" title="تحصيل قسط الاشتراك"><i class='fas fa-coins me-1'></i></a>
                        }
                        else
                        {
                            <button class='btn btn-soft-warning p-2' disabled><i class='fas fa-coins me-1'></i></button>

                        }
                        <a class='btn btn-soft-primary waves-effect waves-light p-2' href="/StudentsClassesTransfer/Index?StudentClassId=@item.Id" title="نقل الطالب من فصل لفصل"><i class='bx bx-transfer'></i></a>
                        <a class='btn btn-soft-secondary waves-effect waves-light p-2' href="/StudentsClass/StudentsLevelsTransfer?Id=@item.Id" title="نقل الطالب من صف لصف"><i class='fa fa-arrow-alt-circle-up'></i></a>
                        <a class='btn btn-soft-info waves-effect waves-light p-2' href="/StudentsClass/StudentsClassPrevious?StudentClassId=@item.Id" title="الالتحاقات السابقة للطالب"><i class='mdi mdi-cards'></i></a>

                        @Url.Edit(routeValues: new { id = item.Id })
                        @Url.Delete(routeValues: new { id = item.Id })

                    </td>
                </tr>
                }
            </tbody>
        </table>

    </div>
</div>
@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
    <script>

        $("#StudyTypeId").change(function () {
            $.get('/StudentsClass/getLevels', { Id: $("#StudyTypeId").val() }, function (data) {
                $("#LevelId").empty();
                $("#LevelId").append("<option>--اختر--</option>")
                $.each(data, function (key, value) {
                    $("#LevelId").append("<option value=" + value["Id"] + ">" + value["Name"] + "</option>")
                });
            });
        });
        $("#LevelId").change(function () {
            $.get('/StudentsClass/getClasses', { Id: $("#LevelId").val() }, function (data) {
                $("#ClassId").empty();
                $("#ClassId").append("<option>--اختر--</option>")
                $.each(data, function (key, value) {
                    $("#ClassId").append("<option value=" + value["Id"] + ">" + value["Name"] + "</option>")
                });
            });
            $.get('/StudentsClass/getSubscriptions', { Id: $("#LevelId").val() }, function (data) {
                $("#SubscriptionId").empty();
                $("#SubscriptionId").append("<option value=''>--اختر--</option>")
                $.each(data, function (key, value) {
                    $("#SubscriptionId").append("<option value=" + value["Id"] + ">" + value["Name"] + "</option>")
                });
            });
        });

    </script>
}