@model IEnumerable<NurseryProject.Dtos.EmployeesDelay.EmployeesDelayDto>
@using NurseryProject.Utilities
@{
    ViewBag.Title = "ادارة تأخيرات الموظفين ";
}
<div class="card">
    <div class="card-header">

        <div class="row">
            <h2 class="col">  </h2>
            <p class="col-auto">
                @Url.Create()
            </p>
        </div>
    </div>
    <div class="card-body">
        <table id="datatable" class="table table-striped dt-responsive  nowrap w-100">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("اسم الموظف")
                    </th>
                    <th>
                        @Html.DisplayName(" تاريخ التأخير")
                    </th>
                    <th>
                        @Html.DisplayName("قيمة التأخير")
                    </th>
                    <th>
                        @Html.DisplayName("سبب التأخير")
                    </th>
                  
                    <th>
                        @Html.DisplayName("عمليات")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmployeeName)
                    </td>
                    <td>
                        @item.Date.Date.ToString("yyyy-MM-dd")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Value)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Reason)
                    </td>
                    <td>
                        @Url.Edit(routeValues: new { id = item.Id })
                        @Url.Delete(routeValues: new { id = item.Id })
                    </td>
                </tr>
                }
            </tbody>
        </table>

    </div>
</div>