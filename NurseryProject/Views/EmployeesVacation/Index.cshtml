@model IEnumerable<NurseryProject.Dtos.EmployeesVacation.EmployeesVacationDto>
@using NurseryProject.Utilities
@{
    ViewBag.Title = "ادارة اجازات الموظفين ";
}
<div class="card">
    <div class="card-header">

        <div class="row">
            <h2 class="col">  </h2>
            <p class="col-auto">
                @Url.Create()
            </p>
        </div>
    </div>
    <div class="card-body">
        <table id="datatable" class="table table-striped dt-responsive  nowrap w-100 table-bordered">
            <thead class="bg-soft-primary">
                <tr>
                    <th>
                        @Html.DisplayName("نوع الاجازة")
                    </th>
                    <th>
                        @Html.DisplayName("اسم الموظف")
                    </th>
                    <th>
                        @Html.DisplayName("من تاريخ")
                    </th>
                    <th>
                        @Html.DisplayName("إلي تاريخ")
                    </th>
                    <th>
                        @Html.DisplayName("سبب الاجازة")
                    </th>

                    <th>
                        @Html.DisplayName("عمليات")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.VacationTypeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmployeeName)
                    </td>
                    <td>
                        @item.DateFrom.Date.ToString("yyyy-MM-dd")
                    </td>
                    <td>
                        @item.DateTo.Date.ToString("yyyy-MM-dd")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Reason)
                    </td>
                    <td>
                        @Url.Edit(routeValues: new { id = item.Id })
                        @Url.Delete(routeValues: new { id = item.Id })
                    </td>
                </tr>
                }
            </tbody>
        </table>

    </div>
</div>