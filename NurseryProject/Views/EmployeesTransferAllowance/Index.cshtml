@model IEnumerable<NurseryProject.Dtos.EmployeesTransferAllowance.EmployeesTransferAllowanceDto>
@using NurseryProject.Utilities
@{
    ViewBag.Title = "ادارة بدلات انتقال الموظفين ";
}
<div class="card">
    <div class="card-header">

        <div class="row">
            <h2 class="col">  </h2>
            <p class="col-auto">
                @Url.Create()
            </p>
        </div>
    </div>
    <div class="card-body">
        <table id="datatable" class="table table-striped dt-responsive  nowrap w-100">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("اسم الموظف")
                    </th>
                    <th>
                        @Html.DisplayName(" تاريخ بدل الانتقال")
                    </th>
                    <th>
                        @Html.DisplayName("قيمة بدل الانتقال")
                    </th>
                    
                    <th>
                        @Html.DisplayName("عمليات")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmployeeName)
                    </td>
                    <td>
                        @item.Date.Date.ToString("yyyy-MM-dd")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Value)
                    </td>
                   
                    <td>
                        @Url.Edit(routeValues: new { id = item.Id })
                        @Url.Delete(routeValues: new { id = item.Id })
                    </td>
                </tr>
                }
            </tbody>
        </table>

    </div>
</div>