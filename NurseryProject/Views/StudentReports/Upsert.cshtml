@model NurseryProject.Dtos.StudentReports.StudentReportsDto
@using NurseryProject.Utilities
@{
    ViewBag.Title = (Model.Id == Guid.Empty ? "إضافة " : "تعديل ") + "  التقرير";
    string action = Model.Id == Guid.Empty ? "Create" : "Edit";
    ViewBag.MainPage = " تقارير الطلاب";

}
@using (Html.BeginForm(action, "StudentReports", FormMethod.Post, new { enctype = "multipart/form-data" }))
{

    <div class="card">
        <div class="card-header">
            <div class="row justify-content-between">
                <h4 class="col-auto">@ViewBag.Title</h4>
                @Url.BackToList()
            </div>
        </div>
        <div class="card-body">
            @Html.HiddenFor(model => model.Id)
            <div class="row">
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("نوع التقرير", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownListFor(model => model.StudentReportTypeId, null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessageFor(model => model.StudentReportTypeId, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("الطالب", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.DropDownListFor(model => model.StudentId, null, "--اختر--", new { @class = "form-control", required = "required" })
                        @Html.ValidationMessageFor(model => model.StudentId, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("من تاريخ", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.EditorFor(model => model.ReportDateFrom, new { htmlAttributes = new { @class = "form-control", @type = "date", required = "required" } })
                        @Html.ValidationMessageFor(model => model.ReportDateFrom, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-6">
                    @Html.Label("الي تاريخ", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.EditorFor(model => model.ReportDateTo, new { htmlAttributes = new { @class = "form-control", @type = "date", required = "required" } })
                        @Html.ValidationMessageFor(model => model.ReportDateTo, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group mb-3 col-sm-12" id="tools">

                </div>
                <div class="form-group mb-3 col-sm-12">
                    @Html.Label("ملاحظات ", htmlAttributes: new { @class = "control-label" })
                    <div class="col-12">
                        @Html.EditorFor(model => model.Notes, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Notes, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <div class="row justify-content-end">
                @if (Model.Id == Guid.Empty)
                {
                    @Url.SubmitAdd()
                }
                else
                {
                    @Url.SubmitSave()
                }
            </div>
        </div>
    </div>
}

@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
    <script>

        $("#StudentReportTypeId").change(function () {
            $.get('/StudentReports/getTools', { Id: $("#StudentReportTypeId").val() }, function (data) {
                $("#tools").empty();
                var j = 0;
                $.each(data, function (key, value) {
                    var t = "<div class='row'><div class='col-sm-6'><label class='control-label'>" + value["Name"] + "</label>";
                    t += "<div class='col-12'><input id='StudentReportItemsDto[" + j + "].StudentReportToolId' name='StudentReportItemsDto[" + j + "].StudentReportToolId' value='"+value["Id"]+"' type='hidden' /><input id='StudentReportItemsDto[" + j + "].Value' name='StudentReportItemsDto[" + j +"].Value' readonly /></div>";
                    t += "</div></div>";
                    $("#tools").append(t)
                    j++;
                });
            });
        });


    </script>
}